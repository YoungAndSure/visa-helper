{
  "name": "visa-helper-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension for visa-helper: in-page form assist + inline material audit",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.41.0",
    "pdfjs-dist": "^4.7.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0",
    "@types/chrome": "^0.0.287",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
