iceland/
.claude/

# audit/ — 每次运行的产物（可能含真实文件名/扫描结果）
audit/report.md

# backend
backend/.venv/
backend/__pycache__/
backend/**/__pycache__/
backend/.pytest_cache/
*.pyc

# extension/ — Vite output and node_modules
# dist/ is committed on purpose: this is a portable-loadable build artifact.
# Don't rsync node_modules across machines — reinstall per env instead.
extension/dist-zip/
extension/node_modules/
extension/.vite-cache/
*.log
.DS_Store
